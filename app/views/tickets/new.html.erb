<h3>Buy your ticket(s) for <%= @concert.headliner %> on <%= @concert.date.strftime("%a %b %e, %Y") %> at <%= @concert.time.strftime("%I:%M %p") %>   </h3>

<%= form_with model: @ticket do | f | %>
    <%= f.label :quantity %>
    <%= f.text_field :quantity %>
    
    <%= f.submit "Purchase" %>
<% end %>